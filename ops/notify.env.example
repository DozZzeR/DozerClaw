# ntfy configuration for DozerClaw health checks. Keep private (chmod 600).
# Set NTFY_URL to your topic URL, e.g. https://ntfy.sh/dozerclaw-2f9k...
# For a private/self-hosted topic, also set NTFY_TOKEN (used as Bearer).
NTFY_URL="https://ntfy.sh/REPLACE_ME"
NTFY_TOKEN=""
